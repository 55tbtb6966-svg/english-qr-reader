# English QR Reader IndexedDB 日本語読み改善版

## 日本語の自然さを改善する方法

CSVに `JapaneseRead` 列を追加しました。

- `Japanese`：画面に表示する日本語
- `JapaneseRead`：実際に読み上げる日本語。ひらがな、空白、句読点で調整します。

例：

ID,English,Japanese,JapaneseRead,Mode,EnglishRate,JapaneseRate,Repeat,Gap,Version
E000001,This is an apple.,これはリンゴです。,これは りんご です。,both,1.0,0.9,1,0.7,2026-08-01

`JapaneseRead` が空欄なら、`Japanese`をそのまま読み上げます。

アプリ内で利用可能な日本語音声も選択できます。


## 漢字列と読み列の表示

読み取り画面では次の両方を表示します。

- 表示用日本語（漢字）：`Japanese`
- 日本語読み：`JapaneseRead`

一括QRカードにも漢字と読みの両方を表示します。


## QRカードの表示内容

一括QR作成画面で、次の3種類から選択できます。

- 英語＋ひらがな
- 英語のみ
- ひらがなのみ

ひらがな表示には `JapaneseRead` を使用します。空欄の場合は `Japanese` を表示します。

## QR読み取り直後の音声改善

カメラ停止直後に約0.5秒待ってから再生し、iOS Safariで先頭音声が途切れにくいようにしました。
また、音声キャンセル直後にも短い待機を入れています。


## QR表示件数とページ切り替え

一括QR作成では、1ページあたり次の件数を選べます。

- 6件
- 8件
- 10件
- 12件
- 20件
- 30件
- 50件

「前のページ」「次のページ」で、選択した件数単位に切り替えられます。


## 音声開始前のポーズ強化

iPhone Safariで先頭音が欠ける場合への対策として、次を追加しました。

- QR読み取り後、カメラ停止から約1秒待って再生
- `speechSynthesis.cancel()`後に約0.35秒待機
- 各音声の直前に無音の準備音＋約0.25秒の待機

以前より再生開始は少し遅くなりますが、先頭音が欠けにくくなります。


## 音声開始時の圧迫音・ポップ音対策

無音の `SpeechSynthesisUtterance` を先に再生する方式を削除しました。
iPhoneでは、無音でも音声出力経路の切り替え時に「ポッ」という音や圧迫感が生じることがあるためです。

現在は次の待機だけを使います。

- カメラ停止後：約0.9秒
- 音声キャンセル後：約0.3秒
- 実際の音声開始前：約0.35秒

無音音声は再生しません。


## 発音記号（IPA）

CSVに `IPA` 列を追加しました。

- 単語・語彙項目：IPAを入力
- 例文：空欄
- 読み取り画面：「発音記号を表示」のON/OFF
- QRカード：「発音記号を表示」のON/OFF
- IPAは読み上げには使用せず、表示専用です

今回のサンプルCSVは米国英語のIPAで統一しています。


## QRカードのID表示

一括QR作成画面で「IDを表示」をON/OFFできます。

- ON：QRコードの下にIDを表示
- OFF：QRコード、英語、ひらがな、発音記号のみ表示

初期設定はONです。


## QR表示設定の配置改善

「発音記号を表示」と「IDを表示」を、同じ設定枠の中に整理しました。

- PC・横画面：横並び
- iPhoneなど狭い画面：縦並び
- チェックボックスの大きさと間隔を統一


## QRサイズと文字サイズ

- QRコード：150pxから132pxへ縮小
- 英語、発音記号、ひらがな、ID：文字を拡大
- 印刷時はQRを124pxに調整し、文字の読みやすさを優先


## 詳細設定（QR・文字サイズ）

QR作成画面の折りたたみ式「詳細設定」から、次を変更できます。

- QRコードサイズ
- ID文字サイズ
- 英語文字サイズ
- 発音記号サイズ
- ひらがな文字サイズ

設定は `localStorage` に保存され、同じiPhone・同じSafariサイトでは次回も維持されます。
「初期値に戻す」で標準サイズへ戻せます。


## 単語1語の開始音対策

iPhoneで単語1語の先頭に「息を吹きかけられたような音」やポップ音を感じる場合への対策です。

- 発話中でない場合は `speechSynthesis.cancel()` を実行しません
- 英単語1語だけの場合、同じ発話の先頭に短い句読点ポーズを入れます
- 単語1語の音量を 0.82 に抑えます
- 別の無音発話は使用しません
- 詳細設定から「単語1語のソフトスタート」をOFFにもできます

これはハウリング対策ではなく、iPhone Safariの音声出力開始時のポップ音・破裂音を目立ちにくくする調整です。


## 短い英語表現にも適用

ソフトスタートの対象を、英単語1語だけでなく次のような短い表現まで広げました。

- `bathtub`
- `Good morning.`
- `washing machine`
- `rice cooker`

対象条件は、英語1～3語かつ概ね28文字以内です。
長い例文には適用しません。


## バージョン表示

画面右上に小さくバージョンを表示します。

- アプリ版：`v1.9.0`
- CSVにVersionがある場合：`App v1.9.0 / CSV 2026-08-01` のように表示
- 印刷時は非表示


## 短い英語に変な言葉が入る問題の修正

以前は短い英語の先頭に `…` を付けて、同じ発話内にポーズを作っていました。
iPhoneの音声によっては、この記号を音声として解釈することがあるため削除しました。

現在は次の処理だけを行います。

- 読み上げる文字列はCSVの英語そのもの
- 短い英語は再生前に約0.65秒待機
- 短い英語の音量は0.9
- 句読点や無音文字などは追加しない


## 英語音声の選択

読み上げ設定に「英語音声」を追加しました。

- iPhone SafariがWebページへ公開している英語音声から選択
- 選択内容は同じiPhoneのSafari内に保存
- 次回起動時も同じ音声を使用
- 日本語音声の選択も保存

iPhone本体に複数の音声があっても、Safari側に公開されない音声は一覧に表示されない場合があります。


## v2.0.0（更新 2026-08-04 20:54）

- 英語音声・日本語音声の選択を「詳細設定」内へ移動
- `playRecord()` を正しい非同期関数へ修正
- QR検出の重複実行を防止
- QR読取後は再生処理の完了まで明示的に待機
- 右上にアプリ版とビルド更新日時を表示
- CSVのVersionがある場合は右上に併記


## v2.0.1（更新 2026-08-04 21:03）

- `async async function playRecord()` の構文ミスを修正
- 容量確認に失敗しても起動処理が止まらないよう修正
- 件数、データ版、保存使用量をそれぞれ独立して取得
- 起動エラー時は画面にエラーを表示


## v2.0.2（更新 2026-08-04 22:58）

- 英語音声・日本語音声の選択を「読み取り」タブへ移動
- QR・文字サイズなどの詳細設定は詳細設定内に維持
- 選択した音声はSafari内に保存


## v2.0.3（更新 2026-08-04 23:08）

- 「短い英語の開始待機」を読み取りタブへ移動
- ON/OFFのすぐ下に簡単な説明を追加
- 英語音声・日本語音声は同じ読み上げ設定内の「詳細設定」に整理
- 選択した設定と音声はSafari内に保存


## v2.0.4（更新 2026-08-04 23:11）

- 「短い英語の開始待機」のON/OFFを詳細設定内へ移動
- 詳細設定に、開始待機・英語音声・日本語音声を集約
- 外側には機能の簡単な説明のみ表示


## v2.0.5（更新 2026-08-04 23:18）

- 読み上げ設定の説明文を詳細設定内へ移動
- 「発音記号を表示」も読み上げ詳細設定内へ移動
- 詳細設定に、発音記号表示・短い英語の開始待機・英語音声・日本語音声を集約
- 発音記号の表示設定はSafari内に保存


## v2.0.6（更新 2026-08-05 01:04）

QRカードに表示する項目を個別にON/OFFできます。

- 英語
- 発音記号
- 日本語
- 日本語読み
- ID

設定はSafari内に保存され、変更すると表示中のQRカードにも反映されます。


## v2.0.7（更新 2026-08-05 01:14）

QR表示項目の選択順をCSV列順に変更しました。

ID → 英語 → 発音記号 → 日本語 → 日本語読み


## v2.0.8（更新 2026-08-05 08:22）

- 読み取りタブの「IDを検索する」ボタンを「確認用ID」の直下へ移動


## v2.0.9（更新 2026-08-07 20:31）

読み取り方法を切り替え可能にしました。

- iPhoneカメラ
- 外部QRスキャナー（Bluetooth/USB HID Keyboard方式）

外部QRスキャナーでは、QR内容をキーボード入力として受信し、EnterでID検索→自動発音します。
推奨設定は HID Keyboard / Instant Upload / Enter(CR) suffix です。


## v2.1.0（更新 2026-08-07 20:35）

- 「QR読み取り方法」を独立枠から移動
- 「QRコードを読み取る」セクション内の「読み取り方法」として配置
- iPhoneカメラ／外部QRスキャナーの切り替え機能はそのまま


## v2.1.1（更新 2026-08-07 20:41）

読み取り方法ごとの操作を明確にしました。

### iPhoneカメラ選択時
- 「QRコードを読み取る」
- 「カメラを止める」
を専用パネルに表示

### 外部QRスキャナー選択時
- カメラ操作ボタンは非表示
- 「スキャン待機中」を表示


## v2.1.2（更新 2026-08-08 09:59）

外部HID QRスキャナー入力のID補正を追加しました。

- え000005 → E000005
- e000005 → E000005
- ｅ000005 → E000005
- Ｅ000005 → E000005

補正は外部QRスキャナー経由の入力時にのみ適用されます。


## v2.1.3（更新 2026-08-08 10:17）

外部QRスキャナー入力の日本語IME対策を強化しました。

- `え000005` → `E000005`
- `エ000005` → `E000005`
- `e000005` → `E000005`
- `Ｅ０００００５` → `E000005`
- 全角数字 → 半角数字
- 不要な空白・改行・タブを除去
- hidden input に `inputmode="latin"` / `lang="en"` / `autocorrect="off"` を追加
- URL形式QRの `?id=` 取り出しにも引き続き対応

※ iOSの入力言語自体をWebアプリから強制変更はせず、受信したIDをアプリ側で正規化します。


## v2.1.4（更新 2026-08-08 16:23）

外部HIDスキャナーの日本語IME対策を方式変更しました。

- `inputmode` に頼らず `KeyboardEvent.code` から物理キーを直接取得
- `KeyE` → `E`
- `Digit0`〜`Digit9` / `Numpad0`〜`Numpad9` → 半角数字
- 日本語IMEが `E` を「え」に変換する前にID文字列を組み立て
- `E` + 6桁が揃った時点で自動検索
- Enterが日本語IMEの確定に使われても検索できる
- `.code` が利用できない場合は従来の文字列正規化へフォールバック


## v2.1.5（更新 2026-08-08 16:35）

QR読み取り方法の初期状態と選択UIを変更しました。

- 初期状態は未選択
- iPhoneカメラ／外部QRスキャナーのどちらかを選んでから開始
- 選択中の方式は枠線・背景・文字色で強調表示
- iPhoneカメラ選択時だけカメラ開始／停止を表示
- 外部QRスキャナー選択時だけ「スキャナーを開始」を表示
- 外部QRスキャナーは開始ボタンを1回タップしてフォーカスを有効化


## v2.1.6（更新 2026-08-08 16:41）

外部QRスキャナーの自動待機復帰を追加しました。

- 最初の「スキャナーを開始」は1回だけ
- QR読み取り・発音後に自動でスキャン待機へ復帰
- アプリ内の画面・タブ切替後に自動でフォーカス復帰
- Safariが前面に戻ったときに自動復帰
- pageshow / window focus / visibilitychange に対応
- 外部QRスキャナー選択中の強調表示は維持
- 読み取り方式をiPhoneカメラへ切り替えた場合は、外部スキャナー開始状態を解除


## v2.1.7（更新 2026-08-08 16:45）

外部QRスキャナーの初回自動発音対策を追加しました。

- 「スキャナーを開始」のユーザー操作内で SpeechSynthesis を初期化
- 無音ウォームアップは使用しない
- 小音量で `Ready` を1回だけ発音して音声エンジンを有効化
- その後スキャン待機へ自動復帰
- 初回QRから設定どおりの自動発音を狙う
- 既存の自動待機復帰機能は維持


## v2.2.0（更新 2026-08-11 15:21）

PowerPoint出力を自作OOXMLからPptxGenJS 4.0.0（ローカル同梱）へ変更。外部CDNへデータを送らず、Safari内で標準PPTXを生成します。


## v2.2.1（更新 2026-08-11 15:25）

PowerPoint生成に必要な JSZip をローカル同梱。読み込み順を jszip.min.js → pptxgen.min.js に修正。


## v2.2.2（更新 2026-08-11 15:30）

PowerPoint保存のレイアウトをPDF表示に近いカード形式へ変更しました。

- 1枚に1個
- 1枚に4個
- 1枚に6個

を選択できます。

現在表示中のQRページを、選択した個数ごとに複数スライドへ分割して出力します。
QRカードの表示項目（ID、英語、発音記号、日本語、日本語読み）のON/OFFを反映します。


## v2.2.3（更新 2026-08-11 15:35）
PowerPointを印刷/PDFと同じA4縦・2列カードレイアウトへ統一。PPT独自の1/4/6個選択は廃止。


## v2.2.4（更新 2026-08-11 15:42）

- 印刷/PDF時に `1～10 / 132件` や `PowerPointを作成しました` などのステータス表示が入らないよう修正
- PowerPointのカード配置を印刷/PDFの見た目へさらに寄せました
- A4縦、2列、3段、カード枠、QRサイズ、英語太字、文字サイズ、行間を調整


## v2.2.6（更新 2026-08-11 15:55）

- 印刷/PDFの「前のページ」「次のページ」を実IDで確実に非表示
- 印刷専用シートを追加
- 印刷範囲を `1-50`、`1,2,4` のように指定可能
- 印刷範囲が空欄なら現在表示中のQRページを印刷


## v2.2.7（更新 2026-08-11 16:03）

QRコード作成の詳細設定に以下を追加しました。

- 日本語文字サイズ
- 日本語読み文字サイズ

それぞれ独立して調整でき、設定はSafari内に保存されます。


## v2.2.8（更新 2026-08-11 16:09）

印刷/PDF専用QRシートでも、QRコード作成の詳細設定にあるサイズをそのまま反映するよう修正しました。

- QRサイズ
- IDサイズ
- 英語サイズ
- 発音記号サイズ
- 日本語サイズ
- 日本語読みサイズ


## v2.3.2（更新 2026-08-11 16:33）

PowerPointのQRサイズを修正しました。

- 詳細設定の現在のQRサイズを基準にPPTへ反映
- 印刷/PDFの124px基準に比例換算
- PPT独自の大きめ固定倍率を廃止


## v2.3.5（更新 2026-08-11 16:53）

A6横のPowerPointが縦向きで開く問題への対策を追加しました。

- PPTX生成後に `ppt/presentation.xml` のスライドサイズを直接修正
- A6横：148 × 105 mm（5328000 × 3780000 EMU）
- A4縦：210 × 297 mm（7560000 × 10692000 EMU）
- ファイル名も `A6_landscape` / `A4_portrait` に変更
